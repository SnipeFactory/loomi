info:
  name: 메모리상황
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:2000/api/memory?action=status
  params:
    - name: action
      value: status
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
