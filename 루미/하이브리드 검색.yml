info:
  name: 하이브리드 검색
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:2000/api/memory?q=루미서비스 기획
  params:
    - name: q
      value: 루미서비스 기획
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
